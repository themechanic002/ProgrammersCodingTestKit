import kotlin.math.*

class Solution61 {
    fun solution(n: Long): Long {
        var answer: Long = 0
        return answer
    }
    fun isSquareRoot(n: Double): Boolean = sqrt(n).toInt().toDouble().pow(2) == n
}